describe("sanity", () => {
    test("true === true", () => {
        expect(true).toEqual(true);
    });
});

describe("handler", () => {
    test("happy path", () => {
        expect(true).toEqual(true);
    });
});
